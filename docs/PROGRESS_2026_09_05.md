# DecodeForge progress — 2026-09-05

The current required deliverable is the Apple M4 eager query-projection demo
defined by [ADR 0005](decisions/0005-prioritize-eager-q-projection-demo.md).
The older P0–P8 cross-ISA scaffold is not the current implementation plan.

## Completion estimate

The required implementation and frozen execution proof now have **4 of 4
accepted gates (100%)**. This counts G0–G3, not publication readiness or optional
G4 extensions. A separate opt-in sentence-formatting run now produces the requested
single sentence; hosted checks and reviewed integration remain delivery work.
Apache 2.0 is implemented for DecodeForge's original code and package metadata.
The earlier 85–90% implementation estimate and 75% gate
count preceded the now-accepted G3 bundle.

| Gate | State | Evidence / remaining work |
| --- | --- | --- |
| G0 semantics | Complete | Independent Python/Rust Q8 oracles, 16-case corpus, retained provenance bundle. |
| G1 compiler/kernel | Complete | Verified IR, packing, scalar/NEON lowering, guarded native execution, three retained M4 sessions. |
| G2 eager integration | Complete | Versioned native bridge, Torch storage execution, ownership, guards, explicit fallback and counters. |
| G3 model proof | Complete under the frozen protocol | Three accepted fresh-process sessions and the independently verified ten-file bundle are checked in. Text quality remains a presentation limitation. |

The retained approximately 3.96x result compares generated NEON with generated
scalar for one projection at the prepared-call boundary. It is not a measured
whole-model speedup. See [the retained G1 result](../results/g1/apple-m4-primary/README.md).

## Outstanding issues, in order

1. **Review and integrate the stack.** Work is separated into
   [seven dependent PRs](REVIEW_STACK.md) above existing #32. Every final split
   head passed local aggregate validation. All hosted checks for #32–#40's open
   PRs subsequently passed. The separate evaluation follow-up has its own review
   and hosted-check lifecycle.
   Nothing has been merged into main, and #33 remains unmerged.
2. **Keep presentation claims narrow.** The default chat-template result still
   has two sentences. A stricter prompt-only attempt failed and is retained;
   explicit `--stop-after-sentence` formatting produced one sentence with 18
   identical tokens, all-22 native coverage, and clean restoration. This is not
   evidence of better unconstrained instruction following. See
   [all presentation outcomes](PRESENTATION_POLISH.md). Frozen G3 is unchanged.
3. **Keep timing claims tied to the measured boundary.** The same-Q8 fallback
   clones and hashes FP32 weights before computing. Projection hooks clone
   inputs/outputs, and isolated native-work timing is unavailable. Report these
   costs, startup and peak memory explicitly when interpreting generation data.
   The [broader evaluation](../results/evaluation/apple-m4-v1/README.md) omits
   comparison hooks from its timers and retains production guards; it does not
   establish a consistent advantage over original FP32.
4. **Clarify the active checkout.** The ordinary local main checkout is stale
   and dirty; it has been preserved. Current work is in the
   `decodeforge-improvements` worktree on `evaluation/v1-results`.
   The remote `improvements-20260905` branch retains #33's original head.
5. **Reproduce the model evaluation on another physical Mac.** An opt-in repair
   can create only the pinned toolchain's expected missing LLVM symlink and
   refuses unexpected entries or path escapes. Completed hosted logs confirm
   that it created the missing link and passed normal/offline preflight plus
   full CI. The local toolchain was already healthy. A separate clean-checkout
   evaluation passed on this same M4, but a second physical host is unavailable.

The broader evaluation passed all 30 correctness cases and three performance
processes from clean source `535540e`, which passed the full aggregate with 497
Python tests. All 30 greedy outputs matched FP32 on the fixed corpus; however,
29 reached their output cap, so natural completion and general task quality
remain unproven. The accepted G3 evidence is unchanged.

Schedule tuning, AVX2, broader linear coverage, general `torch.compile`, fusion
and multicore are optional G4 work after the model evidence, not missing G3
requirements.

## This improvement pass

Work starts from reviewed commit `e4a21da` on branch
`improvements-20260905`. Three Luna agents at high reasoning effort inspect
capture tooling, evidence validation and the model/distribution boundaries;
the parent reviews changes and runs aggregate validation. Existing scaffold
changes are preserved.

Changes in this pass:

- Session CLI rejections now print a concise `g3-session: error:` diagnostic
  and return status 2 instead of an unhandled traceback. A subprocess regression
  proves a malformed bridge identity creates no accepted output.
- G3 release preparation/build targets now run the Rust loader preflight first.
  Behavioral Make tests prove failed preflight prevents the release command.
- Oversized integer timing values now produce a drift-validation diagnostic
  instead of escaping as `OverflowError`.
- Generated G3 README files explicitly describe instrumentation, fallback
  clone/hash costs, unavailable isolated native timing and the measured scope.
- Wheel and sdist validation were checked outside the checkout. Both already
  include and resolve their schema resources; no packaging patch was needed.

Final validation:

| Check | Result |
| --- | --- |
| `CARGO_NET_OFFLINE=true UV_OFFLINE=true make format` | Pass |
| `CARGO_NET_OFFLINE=true UV_OFFLINE=true make check` | Pass: 403 Python tests; Rust debug/release suites; real Torch/dylib execution; fixture parity; G1 report regeneration |
| `make check-rust-toolchain` | Pass on the M4 |
| `actionlint .github/workflows/ci.yml` | Pass |
| `git diff --check` | Pass |
| Wheel and sdist installed outside the checkout | Both pass import and schema validation |

The final aggregate log is retained locally at
`.lavish/improvements-check-final.log`. A first aggregate exposed an existing
command-transport test whose expected invocation count needed to include the
new preflight; the corrected test and final aggregate both pass. The final
Python suite completed in 214.81 seconds.

The follow-on capture found and fixed a lifecycle-evidence projection defect,
then accepted three new sessions from clean revision `ad15f5d`. See the
[capture audit](G3_CAPTURE_AUDIT_2026_09_05.md) and
[result interpretation](G3_RESULT_2026_09_05.md). The fix passed 405 Python tests
and the full aggregate. Both the final G3 bundle and historical G0 provenance
passed verification after the evidence commit.

The presentation follow-up passed the full local `make check`: **422 Python
tests** in 216.25 seconds, Rust debug/release suites, real Torch/native-library
execution, fixture parity, and G1/G3 result verification. Its 17 presentation
tests include pin consistency, real tokenizer mapping compatibility, per-layer
coverage, EOS bounds, lifecycle rejection, and no-overwrite behavior. The log
is `.lavish/presentation-final-check.log`. Documentation/result-retention edits
were pending during this aggregate; the successful model run itself used clean
source `662d137`. Hosted checks subsequently passed and #33 was marked ready.

The final combined source head `1a662b9` passed the offline full `make check`
with **443 Python tests** in 239.27 seconds, Rust debug/release suites, native
execution, fixture parity, and G1/G3 result verification. Its clean-head log is
`.lavish/1a662b9-final-check.log`. Every final split head was also checked
independently; see [the review stack](REVIEW_STACK.md) for counts and revisions.
Three license regressions and actual wheel/sdist inspection verify Apache 2.0
distribution metadata. Four additional presentation cases preserve default
behavior and exercise sentence formatting; 14 repair tests cover guarded
mutation and rejection paths. Luna-high agents reviewed the changes across
subtask boundaries; the parent ran the aggregate validation. Documentation-only
handoff updates follow the tested source head and do not change frozen evidence.
