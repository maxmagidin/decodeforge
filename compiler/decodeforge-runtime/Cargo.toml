[package]
name = "decodeforge-runtime"
version = "0.1.0"
edition = "2024"
publish = false
description = "DecodeForge runtime ownership and generated-module boundary."

[lib]
path = "src/lib.rs"

[target.'cfg(all(target_os = "macos", target_arch = "aarch64"))'.dependencies]
libc = "=0.2.189"
libloading = "=0.9.0"
tempfile = "=3.27.0"

[lints.rust]
unsafe_code = "deny"
