[package]
name = "decodeforge"
version = "0.1.0"
edition = "2024"
license.workspace = true
repository.workspace = true
homepage.workspace = true
publish = false
description = "CLI for DecodeForge contract versions and cross-language Q8 fixture verification."
build = "build.rs"

[[bin]]
name = "decodeforge"
path = "src/main.rs"

[dependencies]
decodeforge-core = { path = "../decodeforge-core" }
decodeforge-runtime = { path = "../decodeforge-runtime" }
